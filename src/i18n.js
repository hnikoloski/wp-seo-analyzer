import { setLocaleData } from '@wordpress/i18n';

// Import translations
const translations = {
    '': {
        domain: 'wp-seo-analyzer',
        lang: 'en_US'
    }
};

setLocaleData(translations, 'wp-seo-analyzer');
